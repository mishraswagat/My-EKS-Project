apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: swagat-cluster
  region: us-east-2
managedNodeGroups:
  - name: ng-swagat-1
    instanceType: t2.large
    desiredCapacity: 3
    minSize: 1
    maxSize: 5
    ssh:
      allow: true # Enables SSH access
